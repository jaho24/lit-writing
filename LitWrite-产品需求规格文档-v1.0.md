# LitWrite — 文献阅读与自动写作助手 产品需求规格文档

> **版本**: v1.0  
> **日期**: 2026-06-02  
> **状态**: 需求已确认  
> **产品代号**: LitWrite

---

## 1. 产品概述

### 1.1 产品定位

LitWrite 是一款面向学术研究者的文献阅读与智能写作辅助工具。它将文献管理、PDF阅读标注、标签化知识组织和AI驱动的学术写作融为一体，帮助研究者从"读论文→做标注→组织观点→写出段落"这一完整链路中大幅提升效率。

### 1.2 核心价值主张

- **一站式闭环**: 从导入文献到生成带引用的学术段落，全程在同一个工具内完成，无需在Zotero、PDF阅读器、笔记工具和写作工具之间反复切换。
- **标注即知识资产**: 每一条标注都通过标签归属到一个观点类别，标注不再是散落在各PDF中的碎片，而是可检索、可组合的结构化知识单元。
- **AI写作对齐逻辑**: 生成的内容不是泛泛的摘要，而是根据用户选定的标签组（代表一组相关观点）和目标写作风格（学术逻辑模式或期刊风格）定向生成的段落，引用信息自动嵌入。

### 1.3 目标用户

| 用户画像 | 核心场景 | 优先级 |
|---|---|---|
| 研究生（硕/博） | 文献综述撰写、开题报告、论文相关工作章节 | P0 |
| 高校教师/研究员 | 课题文献梳理、综述论文撰写、基金申请书论证段落 | P0 |
| 科研机构研究人员 | 系统性文献调研与观点归纳 | P1 |

### 1.4 竞品对比

| 能力维度 | Zotero | ReadPaper | LitWrite |
|---|---|---|---|
| 文献管理与PDF预览 | ✅ 强 | ✅ | ✅ |
| PDF标注（画线/高亮） | ⚠️ 依赖外部PDF阅读器 | ✅ | ✅ 内置 |
| 标注的标签化管理 | ❌ 无 | ⚠️ 有限 | ✅ 核心功能 |
| 按标签组合标注内容 | ❌ | ❌ | ✅ 核心功能 |
| AI学术写作生成 | ❌ | ❌ | ✅ 核心功能 |
| 引用信息自动嵌入 | ✅ 引用格式 | ✅ 引用格式 | ✅ 写作段落内+段后列举 |

**差异化核心**: 标签驱动的标注组织 + AI写作生成是LitWrite独有的组合能力，目前市场上无同类产品覆盖此完整链路。

---

## 2. 功能架构总览

```
┌───────────────────────────────────────────────────────────────┐
│                        LitWrite 功能架构                        │
├───────────┬───────────────┬───────────────┬───────────────────┤
│  文献管理  │   PDF阅读标注  │  标签知识组织  │   写作工作区       │
│           │               │               │                   │
│ · 导入PDF │ · PDF预览     │ · 标注列表     │ · 素材选择(标签组) │
│ · 导入文件夹│ · 高亮标注   │ · 标签分类     │ · 风格三模式       │
│ · 元数据提取│ · 文本批注   │ · 标注-标签关联 │ · (仿写段落)      │
│ · 文献列表 │ · 标注颜色   │ · 标注颜色联动 │ · (期刊风格模板)   │
│ · 分类集合 │   =标签颜色  │ · 跨文献标注检索│ · (自定义提示词)   │
│ · 元数据编辑│              │               │ · 生成输出区      │
│           │               │               │   (段落+引用)     │
│           │               │               │ · 中英双语可选    │
├───────────┴───────────────┴───────────────┴───────────────────┤
│         数据层（SQLite + 文件系统）+ AI层（DeepSeek/Qwen）      │
└───────────────────────────────────────────────────────────────┘
```

---

## 3. 详细功能规格

### 3.1 文献管理模块

#### 3.1.1 文献导入

| 功能点 | 规格说明 | 优先级 |
|---|---|---|
| 单文件PDF导入 | 用户可通过拖拽或文件选择器导入单个PDF文件。系统自动提取元数据并添加到文献库。 | P0 |
| 批量文件夹导入 | 用户可选择一个本地文件夹，系统扫描其中所有PDF文件（含子目录），逐个提取元数据并批量导入。支持进度提示。 | P0 |
| 导入冲突处理 | 当导入的PDF与库中已有文献的文件名或DOI重复时，提示用户选择：跳过 / 替换 / 保留两者。 | P1 |
| 文件存储 | 导入的PDF文件存储在应用数据目录下（按 `library_id/literature_id.pdf` 组织），原始文件不再依赖。 | P0 |

#### 3.1.2 元数据自动提取

导入PDF后，系统自动尝试从PDF内容中提取以下元数据：

| 元数据字段 | 提取策略 | 说明 |
|---|---|---|
| 标题 (title) | 1) 从PDF元数据字段提取；2) 从首页文本通过规则匹配（字体最大、居上居中）推断；3) 用户手动补充 | 三级策略，逐级降级 |
| 作者 (authors) | 1) PDF元数据字段；2) 首页作者行模式匹配（"Author1, Author2 et al."）；3) 手动补充 | 同上 |
| 发表年份 (year) | 1) PDF元数据；2) 首页版权行/日期模式匹配；3) 手动补充 | 同上 |
| 期刊/会议 (journal) | 1) PDF元数据；2) 首页期刊名位置推断；3) 手动补充 | 同上 |
| DOI | 1) PDF全文正则匹配 `10.\d{4,9}/[-._;()/:A-Z0-9]+`；2) 手动补充 | DOI是唯一标识的关键字段 |
| 摘要 (abstract) | 1) 匹配"Abstract"/"摘要"段后的文本块；2) 截取首页前N字符作为降级 | 学术论文通常有明确摘要段 |

**降级原则**: 自动提取可能不完整或错误，所有字段均可手动编辑。系统在提取后标注置信度（高/中/低），置信度低的字段以醒目样式提示用户核实。

#### 3.1.3 文献列表与集合管理

Zotero风格的文献管理交互：

| 功能点 | 规格说明 | 优先级 |
|---|---|---|
| 分类集合树（左侧面板） | 树形结构管理文献集合（类似Zotero的Collections）。支持创建/重命名/删除集合，拖拽文献到集合中。同一文献可归属多个集合。 | P0 |
| 文献列表视图（中间面板） | 表格视图展示当前集合下的所有文献，列可配置（标题、作者、年份、期刊、DOI、添加日期等）。支持排序和筛选。 | P0 |
| 文献详情面板 | 点击文献后在右侧面板展示完整元数据，所有字段可编辑。 | P0 |
| 全库搜索 | 按标题、作者、DOI关键词检索全库文献。 | P1 |
| 文献删除 | 删除文献时同时删除其关联文件、标注、标签关联。支持确认对话框防止误删。 | P0 |

---

### 3.2 PDF阅读与标注模块

#### 3.2.1 PDF预览

| 功能点 | 规格说明 | 优先级 |
|---|---|---|
| PDF渲染 | 使用PDF.js在浏览器内渲染PDF，支持连续滚动视图和单页视图切换。 | P0 |
| 页面缩放 | 支持放大/缩小/适应宽度/适应页面。 | P0 |
| 页面导航 | 页码跳转、上下页按钮、缩略图侧栏快速定位。 | P1 |
| 文本选择 | 用户可选择PDF中的文本，作为标注操作的前提。 | P0 |

#### 3.2.2 标注操作

标注是本产品最核心的数据单元，定义如下：

```
标注(Annotation) = {
  id: 唯一标识,
  literature_id: 所属文献,
  page: 页码,
  position: { x, y, width, height },   // 在PDF页面上的位置矩形
  type: 'highlight' | 'note',          // MVP仅支持高亮和批注两种类型
  color: 由关联标签颜色派生，           // 标注颜色 = 第一个关联标签的颜色
                                      // 无标签时显示默认灰色(#9E9E9E)
                                      // 有多个标签时显示第一个标签的颜色
  text: 被标注的原始文本内容,
  note: 用户附加的批注文本,
  tags: [关联的标签列表],              // 标签颜色决定标注在PDF上的视觉颜色
  created_at: 创建时间,
  updated_at: 更新时间
}
```

| 功能点 | 规格说明 | 优先级 |
|---|---|---|
| 高亮标注 (Highlight) | 用户选择文本后，点击高亮按钮，文本区域覆盖半透明色块。标注颜色自动跟随其关联标签的颜色（见3.3标签颜色联动）。未分配标签时显示默认灰色。 | P0 |
| 文本批注 (Note) | 在标注上附加自由文本笔记。点击标注后弹出批注编辑框。 | P0 |
| 标注颜色联动 | **标注颜色 = 标签颜色**：标注的视觉颜色由其关联的第一个标签的颜色决定。修改标签颜色时，该标签下的所有标注颜色同步更新。无标签的标注显示默认灰色。 | P0 |
| 标注删除 | 右键/长按标注弹出菜单，选择删除。删除时同时解除其标签关联。 | P0 |
| 标注编辑 | 修改批注内容、调整标注范围。颜色不可手动修改（由标签决定）。 | P1 |
| 标注持久化 | 所有标注数据存储在数据库中，重新打开PDF时标注完整还原到对应位置。 | P0 |
| 标注导出 | 可将单篇文献的所有标注导出为文本列表（标注原文 + 批注 + 标签）。 | P2 |

#### 3.2.3 标注交互细节

- **创建标注的流程**: 选择文本 → 浮动工具栏出现（高亮按钮）→ 点击高亮 → 标注立即渲染在PDF上（灰色，尚未分配标签）→ 弹出标签选择面板 → 选择标签后标注颜色自动变为标签颜色 → 完成
- **查看标注**: 点击已有标注 → 弹出信息卡片，显示：原文、批注、所属标签列表（标签带颜色标记）、时间。可在此编辑批注、添加/移除标签。
- **颜色联动规则**: 标注颜色 = 第一个关联标签的颜色。添加标签后颜色立即更新；移除所有标签后恢复灰色；修改标签颜色后该标签下所有标注颜色同步刷新。
- **标注与PDF渲染**: 标注作为SVG/Canvas覆盖层叠加在PDF.js渲染层之上，不修改原始PDF文件。

---

### 3.3 标签知识组织模块

标签系统是将散落的标注组织为结构化知识的核心机制。

#### 3.3.1 标签定义与管理

| 功能点 | 规格说明 | 优先级 |
|---|---|---|
| 标签创建 | 用户创建标签时定义：名称、颜色、描述。标签名称不可重复。 | P0 |
| 标签列表 | 侧栏或独立面板展示所有标签，按名称排序或按标注数量排序。每个标签显示颜色标记、名称、标注计数。 | P0 |
| 标签编辑 | 修改标签名称、颜色、描述。修改后所有关联标注同步更新显示。 | P0 |
| 标签删除 | 删除标签时选择：仅解除关联（标注保留但无此标签） / 同时删除标注。默认仅解除关联。 | P0 |
| 标签颜色 | 每个标签有独立颜色，**标签颜色决定其下所有标注在PDF上的视觉颜色**（标注颜色=标签颜色）。修改标签颜色→该标签下所有标注颜色同步刷新。 | P0 |
| 标签层级（可选） | 支持标签嵌套（如"方法论 > 实验设计"），形成标签树。 | P2 |

#### 3.3.2 标注-标签关联

| 功能点 | 规格说明 | 优先级 |
|---|---|---|
| 为标注添加标签 | 创建标注后立即弹出标签选择面板；也可后续点击标注再添加。支持多标签（一个标注可归属多个标签）。 | P0 |
| 移除标注的标签 | 在标注信息卡片中移除某个标签关联。 | P0 |
| 批量标注标签 | 在标注列表视图中多选标注，批量添加/移除标签。 | P1 |
| 按标签筛选标注 | 选择一个或多个标签，筛选出所有关联标注。支持AND/OR逻辑：AND=同时拥有所有选中标签的标注；OR=拥有任一选中标签的标注。 | P0 |

#### 3.3.3 跨文献标注视图

这是标签系统的核心价值——打破文献边界，按观点类别聚合标注：

| 功能点 | 规格说明 | 优先级 |
|---|---|---|
| 标签标注视图 | 选择一个标签后，展示所有归属该标签的标注，无论来自哪篇文献。每条标注显示：原文、批注、来源文献标题和作者、页码。 | P0 |
| 多标签组合视图 | 选择多个标签后，展示同时拥有这些标签的标注（AND逻辑），或拥有任一标签的标注（OR逻辑）。 | P0 |
| 从标注跳转到PDF | 在跨文献标注视图中点击某条标注，自动打开对应文献的PDF并定位到标注位置。 | P0 |
| 标注排序 | 跨文献视图中的标注可按：来源文献排序、创建时间排序、标注颜色排序。 | P1 |

---

### 3.4 写作工作区模块

写作工作区是产品的核心输出环节，将标签组织的标注内容转化为带引用的学术段落。工作区不是一次性弹窗，而是持久的写作面板，支持素材→风格→输出的三区流转，且生成的段落可累积编排。

#### 3.4.1 工作区三区架构

写作工作区由三个功能区构成，从左到右依次操作：

```
┌──────────────┬──────────────────┬──────────────────┐
│  素材选择区    │  写作风格区       │  生成输出区       │
│  (标签→标注)   │  (三模式选一)     │  (段落+引用)     │
└──────────────┴──────────────────┴──────────────────┘
```

| 区域 | 功能 | 操作内容 |
|---|---|---|
| **素材选择区** | 根据标签选择写作素材来源 | 勾选1-N个标签 → 系统聚合这些标签下的所有标注 → 预览标注数量和来源文献数量 → 可展开查看具体标注内容 |
| **写作风格区** | 选择AI写作的指令模式 | 三种模式（3.4.2详述），选其一或组合：①仿写段落 ②期刊风格模板 ③自定义提示词。另设输出语言（中/英） |
| **生成输出区** | 展示AI生成的段落及引用 | 生成的段落文本（内嵌[编号]引用标记）+ 段后参考文献列表。支持复制、重新生成、编辑、导出 |

#### 3.4.2 写作风格三模式

写作风格区提供三种互不排斥的模式，用户选择其一为主要指令，也可组合使用：

**模式一：仿写段落**

用户提供一段已有的学术文本作为仿写对象，AI分析其写作逻辑并模仿生成新段落。

| 输入方式 | 说明 |
|---|---|
| 从库中选择 | 从文献库中选择某篇文献的摘要或某段文本，系统提取该文本的论证结构、语气密度、句式模式和引用嵌入方式，作为生成指令的一部分 |
| 外部粘贴 | 用户从外部（自己写的草稿、他人论文片段等）粘贴一段文本，系统同样分析其写作风格特征 |

AI提取的文本特征维度：
- 论证结构（如：现象→问题→方法→结论、背景→分论点→展望）
- 语气密度（客观陈述 vs 论证性论述 vs 评述性）
- 句式模式（长短句比例、从句嵌套深度、被动语态比例）
- 引用嵌入方式（括号数字[1] vs 作者年份(Smith, 2023) vs 脚注风格）

**模式二：期刊风格模板**

选择预置的学术写作风格模板，每个模板内置完整的写作逻辑指令和引用格式：

```
写作风格(WritingStyle) = {
  id: 唯一标识,
  name: 风格名称,
  description: 风格描述,
  style_prompt: 对AI模型的指令文本，定义期望的写作逻辑、语气、结构和引用格式,
  citation_format: 引用格式（APA / IEEE / GB/T 7714 / Nature等）,
  language: 输出语言（中文/英文）
}
```

预置风格模板（用户可自定义扩展）：

| 风格名 | 逻辑模式 | 引用格式 | 语言 |
|---|---|---|---|
| Nature综述风格 | 概述→分论点→展望，每论点引1-2篇 | Nature括号数字 | 英文 |
| IEEE论证段落 | 背景→问题→现有方法→评述→改进方向 | IEEE方括号数字 | 英文 |
| 社科定性论述 | 现象描述→理论框架→案例支撑→反思 | GB/T 7714 | 中文 |
| 实验方法比较 | 问题→方法A→方法B→对比→结论 | APA | 英文 |
| 中文文献综述 | 研究背景→国内外现状→争议→趋势 | GB/T 7714 | 中文 |

**模式三：自定义提示词**

用户直接输入写作指令，最灵活的控制方式。适合对写作逻辑有明确设想的研究者。

```
示例自定义提示词：
"请用对比论证的方式，先概述方法A的核心思路和优势，再概述方法B的核心思路和优势，
最后比较两者的适用场景和局限性。引用格式用GB/T 7714，输出中文。"
```

自定义提示词直接作为AI的 `style_prompt` 使用，用户拥有完全的写作方向控制权。系统仅在提示词末尾追加引用格式和语言要求（如用户未在提示词中指定）。

**模式组合规则**：
- 三种模式选其一为主——不可同时选仿写段落和期刊风格模板（它们的逻辑可能冲突）
- 自定义提示词可与仿写段落或期刊风格组合使用：仿写/期刊提供风格骨架，自定义提示词补充额外要求（如"重点关注实验结果的对比"）

#### 3.4.3 生成输出格式

AI生成的写作段落结构：

```
生成的段落 = {
  content: "生成的学术段落文本，内嵌引用标记",
  citations: [
    {
      marker: "[1]",           // 段落中的引用标记
      literature_id: 对应文献ID,
      title: "文献标题",
      authors: "作者列表",
      year: 发表年份,
      journal: "期刊名",
      doi: "DOI"
    },
    ...
  ],
  style_used: 使用的写作风格信息,
  tags_used: 使用的标签组信息,
  annotation_count: 涉及的标注总数,
  generated_at: 生成时间
}
```

**段落内引用标记**: 在生成的文本中，引用信息以标记嵌入（如 `[1]`、`[2]`），与文末引用列表对应。

**段后引用列举**: 段落正文结束后，自动列举所有引用文献的完整信息，格式按所选风格的 `citation_format` 渲染。

示例输出（中文文献综述风格）：

> 近年来，深度学习在医学影像分析中取得了显著进展 [1]。Zhang等人提出的卷积神经网络架构在肺结节检测任务上达到了92.3%的敏感度 [2]，而Li等基于注意力机制的模型则在小病灶识别方面表现更优 [3]。然而，这些方法在跨数据集泛化能力上仍存在不足 [1][4]，数据偏移和标注不一致是主要瓶颈 [4]。

> **参考文献**  
> [1] Wang, Y. et al. Deep learning for medical image analysis: A review. *Nature Medicine*, 2023. DOI: 10.1038/xxx  
> [2] Zhang, H. et al. CNN-based lung nodule detection. *IEEE TMI*, 2022. DOI: 10.1109/xxx  
> [3] Li, J. et al. Attention mechanisms for small lesion identification. *Medical Image Analysis*, 2023. DOI: 10.1016/xxx  
> [4] Chen, R. et al. Cross-dataset generalization challenges in medical AI. *Lancet Digital Health*, 2024. DOI: 10.1016/xxx

#### 3.4.4 写作工作区交互流程

1. 用户点击"写作工作区"Tab或顶部按钮 → 打开写作工作区面板（三区布局）
2. **素材选择区**：勾选1-N个标签 → 系统实时显示：涉及标注总数、来源文献总数 → 可点击展开查看具体标注内容列表 → 确认素材范围
3. **写作风格区**：选择风格模式
   - 仿写段落：从库中选择文献段落 / 粘贴外部文本 → AI自动分析其写作特征 → 显示分析结果摘要（"该文本采用对比论证结构，语气偏评述性，引用嵌入为作者年份格式"）
   - 期刊风格：下拉选择预置模板 → 显示模板描述
   - 自定义提示词：在文本框中输入写作指令 → 可选追加补充要求
   - 设定输出语言（中文/英文）
4. 点击"生成" → 系统将素材标注内容 + 风格指令 + 引用格式要求 + 语言要求组合为AI prompt → 调用DeepSeek/Qwen API → 返回生成结果
5. **生成输出区**：展示生成的段落文本 + 参考文献列表
6. 用户操作：
   - **复制**：一键复制段落+引用到剪贴板
   - **重新生成**：调整参数后再次生成
   - **编辑**：在输出区内直接编辑生成文本
   - **导出**：导出为Markdown / 纯文本格式

#### 3.4.5 AI Prompt构造策略

系统构造发送给大模型的prompt包含以下部分：

### 10.3 DeepSeek/Qwen Prompt适配

针对国内大模型API的调用，Prompt构造策略需适配DeepSeek/Qwen的API特性：

1. DeepSeek API兼容OpenAI格式（`/v1/chat/completions`），接口），可直接使用OpenAI SDK的调用方式，只需修改base URL和model name。
2. Qwen（通义千问）API使用不同的调用方式，需要适配其SDK或直接HTTP请求。

3. 架构层统一封装为AI Service抽象层，根据配置切换具体实现。

```

[System Instruction]
你 是一位学术写作助手。根据用户提供的标注素材和写作风格要求，生成一段学术论述段落。
要求：
1. 严格基于提供的标注素材内容，不添加标注中未涉及的信息
2. 每个论点必须引用对应的标注来源文献,使用[编号]标记
3. 段落逻辑和语气对齐指定的写作风格
4. 引用格式严格按指定规范
5. 输出语言按指定要求

6. 生成内容为一段完整的论述段落（非综述片段模式）

[Writing Style]
{style_prompt 或从参考文本分析得出的风格指令}

（参考文本分析逻辑：当用户选择库内文献段落或粘贴外部文本时，系统提取该文本的论证结构、语气密度、句式模式和引用嵌入方式，并在style_prompt中体现）

）

[Annotation Materials]
以下是按标签组织的标注素材：

{按标签分组，每个标签下的标注按来源文献排列}
标签 "{tag_name_1}" 下的标注：
- 来源：{literature_title} ({authors}, {year})，DOI: {doi}，第{page}页
  原文："{annotation_text}"
  批注："{annotation_note}"

标签 "{tag_name_2}" 下的标注：
...（同上格式）

[Citation Format Requirement]
引用格式：{citation_format_name}
引用标记：正文中用[{序号}]，段后列举完整引用信息

[Output Language]
{language}

---

## 4. UI/交互设计规格

### 4.1 整体布局（Zotero风格三栏）

```
┌─────────────────────────────────────────────────────────────────────┐
│  顶部工具栏：搜索 | 导入 | 写作工作区 | 设置                            │
├──────────┬───────────────────┬──────────────────────────────────────┤
│          │                   │                                      │
│  集合树   │   文献列表         │   右侧主面板                          │
│  (240px) │   (320px)         │   (自适应剩余宽度)                    │
│          │                   │                                      │
│  · 我的库 │ ┌───────────────┐ │  ┌─ Tab切换 ─────────────────────┐  │
│  · 集合A │ │ 标题│作者│年份  │ │  │ PDF预览 | 标注列表 | 写作工作区 │  │
│  · 集合B │ │ ────────────── │ │  ├───────────────────────────────┤  │
│  · 集合C │ │ Paper1│...│2023│ │  │                               │  │
│          │ │ Paper2│...│2022│ │  │  (PDF预览时：渲染PDF +         │  │
│  ─────── │ │ Paper3│...│2024│ │  │   标注覆盖层 + 选择工具栏)     │  │
│  标签列表 │ │               │ │  │                               │  │
│  🔴方法  │ └───────────────┘ │  │  (标注列表时：按标签分组展示     │  │
│  🟢结果  │                   │  │   所有标注，支持筛选)           │  │
│  🔵理论  │                   │  │                               │  │
│          │                   │  │  (写作工作区：素材区+风格区      │  │
│          │                   │  │   +生成输出区三区布局)          │  │
│          │                   │  └───────────────────────────────┘  │
├──────────┴───────────────────┴──────────────────────────────────────┤
│  底部状态栏：文献总数 | 标注总数 | 当前集合名                          │
└─────────────────────────────────────────────────────────────────────┘
```

### 4.2 面板切换逻辑

右侧主面板通过Tab切换三种视图：

| Tab | 触发条件 | 展示内容 |
|---|---|---|
| PDF预览 | 点击文献列表中的某篇文献 | 该文献的PDF全文渲染，标注叠加显示 |
| 标注列表 | 点击左侧标签列表中的某个标签；或从PDF预览中切换 | 跨文献标注聚合视图 |
| 写作工作区 | 点击顶部"写作工作区"按钮或切换Tab | 写作工作区面板（素材区+风格区+输出区） |

### 4.3 关键交互细节

#### PDF标注创建交互

```
用户操作流程：
1. 在PDF预览中选择一段文字
2. 选择文字后，在文字上方浮现迷你工具栏：
   ┌──────────────────────────────────┐
   │ 🔖高亮 │ _下划线 │ ✘删除线 │ 📝批注 │
   └──────────────────────────────────┘
3. 点击"高亮" → 文字区域覆盖半透明色块（默认黄色）
4. 标注创建后，右侧弹出标签分配面板：
   ┌────────────────────┐
   │ 选择标签：          │
   │ ☑ 🔴 方法论        │
   │ ☐ 🟢 实验结果      │
   │ ☐ 🔵 理论框架      │
   │ + 创建新标签        │
   └────────────────────┘
5. 选择标签后标注完成，标注数据持久化
```

#### 标注信息查看交互

```
点击已有标注 → 弹出标注信息卡片：
┌─────────────────────────────┐
│ 📌 标注详情                  │
│─────────────────────────────│
│ 原文："深度学习在医学影像..." │
│ 批注：[用户可编辑的批注区]    │
│─────────────────────────────│
│ 标签：🔴方法论  🟢实验结果    │
│       [+添加标签]            │
│─────────────────────────────│
│ 来源：Zhang et al., 2022     │
│ 页码：第3页                  │
│ 时间：2024-01-15             │
│─────────────────────────────│
│ [编辑] [删除]                │
└─────────────────────────────┘
```

#### 写作工作区交互

```
写作工作区面板（三区布局）：
┌──────────────────────────────────────────────────────────────────┐
│ 📝 写作工作区                                                     │
├─────────────┬──────────────────────┬──────────────────────────────┤
│ ① 素材选择区 │ ② 写作风格区          │ ③ 生成输出区                  │
│             │                      │                              │
│ 选择标签组：  │ 风格模式（三选一）：    │ (生成后展示)                  │
│             │                      │                              │
│ ☑ 🔴方法论   │ ▸ 仿写段落            │ 近年来，深度学习在医学影像分析 │
│   12条标注   │   · 从库中选择文献段落 │ 中取得了显著进展 [1]...        │
│ ☑ 🟢实验结果 │   · 粘贴外部参考文本   │ Zhang等人提出的...[2]          │
│   8条标注    │   [文本输入区/选择器]  │                              │
│ ☐ 🔵理论框架 │                      │ ─── 参考文献 ───              │
│   5条标注    │ ▸ 期刊风格            │ [1] Wang, Y. et al. ...       │
│             │   ▾ 中文文献综述       │ [2] Zhang, H. et al. ...      │
│ ───────     │   · Nature综述风格    │ [3] Li, J. et al. ...         │
│ 素材概览：    │   · IEEE论证段落      │                              │
│ 涉及5篇文献  │   · 社科定性论述       │ ─── 操作 ───                  │
│ 20条标注    │                      │ [复制] [重新生成] [编辑]        │
│             │ ▸ 自定义提示词        │ [导出Markdown] [导出纯文本]    │
│ [展开查看    │   [自由输入写作指令]   │                              │
│  具体标注]   │   例："用对比论证...   │                              │
│             │                      │                              │
│             │ ─── 通用设置 ───      │                              │
│             │ 输出语言：▾ 中文       │                              │
│             │ 引用格式：▾ GB/T 7714 │                              │
├─────────────┴──────────────────────┴──────────────────────────────┤
│                        [ 生成 ]  [ 清空 ]                          │
└──────────────────────────────────────────────────────────────────┘
```

---

## 5. 数据模型

### 5.1 数据库表结构（SQLite）

```sql
-- 文献集合（类似Zotero Collections）
CREATE TABLE libraries (
  id            INTEGER PRIMARY KEY AUTOINCREMENT,
  name          TEXT NOT NULL,
  parent_id     INTEGER REFERENCES libraries(id) ON DELETE SET NULL,
  created_at    TEXT DEFAULT (datetime('now')),
  updated_at    TEXT DEFAULT (datetime('now'))
);

-- 文献条目
CREATE TABLE literature (
  id            INTEGER PRIMARY KEY AUTOINCREMENT,
  title         TEXT,
  authors       TEXT,           -- 逗号分隔的作者列表
  year          INTEGER,
  journal       TEXT,
  doi           TEXT,
  abstract      TEXT,
  file_path     TEXT NOT NULL,  -- 存储在应用数据目录下的相对路径
  file_name     TEXT NOT NULL,  -- 原始文件名
  library_id    INTEGER REFERENCES libraries(id) ON DELETE SET NULL,
  metadata_confidence TEXT,     -- JSON: {"title":"high","authors":"medium",...}
  added_at      TEXT DEFAULT (datetime('now')),
  updated_at    TEXT DEFAULT (datetime('now'))
);

-- 文献-集合多对多关联（同一文献可归属多个集合）
CREATE TABLE literature_libraries (
  literature_id INTEGER NOT NULL REFERENCES literature(id) ON DELETE CASCADE,
  library_id    INTEGER NOT NULL REFERENCES libraries(id) ON DELETE CASCADE,
  PRIMARY KEY (literature_id, library_id)
);

-- 标注
CREATE TABLE annotations (
  id            INTEGER PRIMARY KEY AUTOINCREMENT,
  literature_id INTEGER NOT NULL REFERENCES literature(id) ON DELETE CASCADE,
  page          INTEGER NOT NULL,
  position_x    REAL,           -- PDF页面上的矩形位置（百分比坐标，0-1）
  position_y    REAL,
  width         REAL,
  height        REAL,
  color         TEXT DEFAULT '#9E9E9E',  -- 默认灰色，实际颜色由标签派生（前端渲染时动态计算）
  type          TEXT DEFAULT 'highlight' CHECK(type IN ('highlight','note')),
  text          TEXT,           -- 被标注的原文
  note          TEXT,           -- 用户附加批注
  created_at    TEXT DEFAULT (datetime('now')),
  updated_at    TEXT DEFAULT (datetime('now'))
);

-- 标签
CREATE TABLE tags (
  id            INTEGER PRIMARY KEY AUTOINCREMENT,
  name          TEXT NOT NULL UNIQUE,
  color         TEXT DEFAULT '#4CAF50',
  description   TEXT,
  parent_id     INTEGER REFERENCES tags(id) ON DELETE SET NULL,
  created_at    TEXT DEFAULT (datetime('now'))
);

-- 标注-标签多对多关联
CREATE TABLE annotation_tags (
  annotation_id INTEGER NOT NULL REFERENCES annotations(id) ON DELETE CASCADE,
  tag_id        INTEGER NOT NULL REFERENCES tags(id) ON DELETE CASCADE,
  PRIMARY KEY (annotation_id, tag_id)
);

-- 写作风格模板
CREATE TABLE writing_styles (
  id            INTEGER PRIMARY KEY AUTOINCREMENT,
  name          TEXT NOT NULL,
  description   TEXT,
  style_prompt  TEXT NOT NULL,  -- AI模型指令文本
  citation_format TEXT NOT NULL, -- 引用格式名称（APA/IEEE/GB-T-7714/Nature等）
  language      TEXT DEFAULT 'zh',
  is_builtin    INTEGER DEFAULT 0, -- 1=预置风格，0=用户自定义
  created_at    TEXT DEFAULT (datetime('now'))
);

-- 生成记录（历史）
CREATE TABLE generation_records (
  id            INTEGER PRIMARY KEY AUTOINCREMENT,
  content       TEXT NOT NULL,   -- 生成的段落文本
  citations     TEXT NOT NULL,   -- JSON: 引用文献列表
  style_id      INTEGER REFERENCES writing_styles(id),
  reference_text TEXT,           -- 如果使用参考文本对齐模式
  tags_used     TEXT NOT NULL,   -- JSON: 使用的标签ID列表
  annotation_ids TEXT,           -- JSON: 涉及的标注ID列表
  language      TEXT DEFAULT 'zh',
  generated_at  TEXT DEFAULT (datetime('now'))
);
```

### 5.2 文件存储结构

```
app_data/
├── pdfs/                        -- 存储导入的PDF文件
│   ├── {literature_id}/
│   │   └── {file_name}.pdf
├── database/
│   └── litwrite.db              -- SQLite数据库文件
└── config/
│   └── settings.json            -- 应用配置（AI API key等）
```

---

## 6. 技术架构

### 6.1 技术栈选型

| 层级 | 技术 | 选型理由 |
|---|---|---|
| 前端框架 | React 18 + TypeScript | 组件生态丰富，PDF.js/Annotation库支持好 |
| 前端构建 | Vite | 开发体验快，HMR即时 |
| 状态管理 | Zustand | 轻量、直观，适合中等复杂度应用 |
| 样式方案 | TailwindCSS | 快速原型+一致设计系统 |
| PDF渲染 | pdfjs-dist (PDF.js) | Mozilla官方库，渲染质量可靠，支持文本层交互 |
| 标注覆盖层 | 自实现SVG/Canvas层 | PDF.js无内置标注编辑，需自实现标注渲染和交互层 |
| 图标库 | Lucide React | 轻量、一致的图标集 |
| HTTP客户端 | Axios | 前端API调用 |
| 后端框架 | Express + TypeScript | Node.js生态成熟，前后端语言统一 |
| 数据库 | better-sqlite3 | 嵌入式数据库，无需外部依赖，单文件部署 |
| PDF解析 | pdf-parse | Node.js下提取PDF文本和元数据 |
| 文件上传 | multer | Express标准文件上传中间件 |
| AI API | DeepSeek API / Qwen API（通义千问） | 国内大模型，直接可访问无需海外代理；写作质量优秀，价格低于GPT-4；架构预留多模型接口 |
| 跨域 | cors | 前后端分离开发必需 |

### 6.2 系统架构图

```
┌─────────────────────────────────────────────────────────────┐
│                     React 前端应用                            │
│  ┌──────┐ ┌──────────┐ ┌───────────┐ ┌──────────────────┐ │
│  │集合树 │ │文献列表   │ │PDF预览+标注│ │写作生成面板      │ │
│  └──────┘ └──────────┘ └───────────┘ └──────────────────┘ │
│         ↕ Zustand Store ↕ Axios API Client                  │
├─────────────────────────────────────────────────────────────┤
│                     Express 后端 API                         │
│  ┌──────┐ ┌──────────┐ ┌───────────┐ ┌──────────────────┐ │
│  │/lib   │ │/literature│ │/annotation │ │/generation      │ │
│  │/tag   │ │/upload    │ │/writing-   │ │/styles          │ │
│  │       │ │/scan-     │ │style       │ │                 │ │
│  │       │ │folder     │ │            │ │                 │ │
│  └──────┘ └──────────┘ └───────────┘ └──────────────────┘ │
│         ↕ Services ↕                                         │
│  ┌───────────┐ ┌────────────┐ ┌──────────────────────────┐│
│  │PDF解析服务 │ │文件扫描服务 │ │AI写作生成服务             ││
│  │(元数据提取)│ │(文件夹导入) │ │(DeepSeek/Qwen API调用)    ││
│  └───────────┘ └────────────┘ └──────────────────────────┘│
├─────────────────────────────────────────────────────────────┤
│           better-sqlite3 + 本地文件系统                       │
└─────────────────────────────────────────────────────────────┘
```

### 6.3 API设计

#### 文献管理API

| 端点 | 方法 | 说明 |
|---|---|---|
| `/api/libraries` | GET | 获取所有集合（树形结构） |
| `/api/libraries` | POST | 创建集合 |
| `/api/libraries/:id` | PUT | 更新集合（重命名、移动） |
| `/api/libraries/:id` | DELETE | 删除集合 |
| `/api/literature` | GET | 获取文献列表（可按集合筛选、搜索） |
| `/api/literature/:id` | GET | 获取单篇文献详情 |
| `/api/literature/:id` | PUT | 更新文献元数据 |
| `/api/literature/:id` | DELETE | 删除文献 |
| `/api/literature/:id/pdf` | GET | 获取PDF文件（供前端预览） |
| `/api/upload/pdf` | POST | 上传单个PDF文件，自动提取元数据 |
| `/api/upload/folder` | POST | 批量扫描文件夹并导入PDF |
| `/api/literature/:id/libraries` | PUT | 设置文献归属的集合列表 |

#### 标注与标签API

| 端点 | 方法 | 说明 |
|---|---|---|
| `/api/literature/:id/annotations` | GET | 获取某文献的所有标注 |
| `/api/annotations` | GET | 跨文献查询标注（按标签筛选） |
| `/api/annotations` | POST | 创建标注 |
| `/api/annotations/:id` | PUT | 更新标注（修改批注、颜色等） |
| `/api/annotations/:id` | DELETE | 删除标注 |
| `/api/annotations/:id/tags` | PUT | 设置标注的标签关联 |
| `/api/tags` | GET | 获取所有标签 |
| `/api/tags` | POST | 创建标签 |
| `/api/tags/:id` | PUT | 更新标签 |
| `/api/tags/:id` | DELETE | 删除标签 |
| `/api/tags/:id/annotations` | GET | 获取某标签下的所有标注 |

#### 写作生成API

| 端点 | 方法 | 说明 |
|---|---|---|
| `/api/writing-styles` | GET | 获取所有写作风格模板 |
| `/api/writing-styles` | POST | 创建自定义写作风格 |
| `/api/writing-styles/:id` | PUT | 更新写作风格 |
| `/api/writing-styles/:id` | DELETE | 删除自定义风格（预置不可删） |
| `/api/generate` | POST | 执行写作生成 |
| `/api/generation-records` | GET | 获取生成历史 |
| `/api/generation-records/:id` | GET | 获取某次生成详情 |

**`/api/generate` POST请求体**:
```json
{
  "tag_ids": [1, 3, 5],
  "style_mode": "journal_style",       // 三种模式："imitate" | "journal_style" | "custom_prompt"
  "style_id": 2,                        // journal_style模式时必填
  "reference_text": null,               // imitate模式时必填（仿写段落文本）
  "reference_source": "external",       // imitate模式的来源："library" | "external"
  "reference_literature_id": null,      // reference_source=library时的文献ID
  "custom_prompt": null,                // custom_prompt模式时必填（自定义提示词）
  "language": "zh",                     // 输出语言：zh | en
  "citation_format": "GB/T 7714"        // 引用格式（可由风格模板自动决定，也可手动指定）
}
```

**`/api/generate` POST响应体**:
```json
{
  "id": 42,
  "content": "近年来，深度学习在医学影像分析中取得了显著进展 [1]...",
  "citations": [
    {
      "marker": "[1]",
      "literature_id": 15,
      "title": "Deep learning for medical image analysis",
      "authors": "Wang, Y. et al.",
      "year": 2023,
      "journal": "Nature Medicine",
      "doi": "10.1038/xxx"
    }
  ],
  "style_used": { "id": 2, "name": "中文文献综述" },
  "tags_used": [1, 3, 5],
  "annotation_count": 20,
  "generated_at": "2024-01-20T14:30:00Z"
}
```

---

## 7. 非功能性需求

| 类别 | 需求 | 指标 |
|---|---|---|
| 性能 | PDF预览加载速度 | 50页PDF ≤3秒完成渲染 |
| 性能 | 文献列表响应 | 1000条文献 ≤500ms列表加载 |
| 性能 | 写作生成延迟 | ≤30秒返回结果（依赖AI API） |
| 可靠性 | 标注数据持久化 | 100%不丢失，页面刷新后完整还原 |
| 可靠性 | PDF文件完整性 | 导入的PDF文件不修改，标注为附加数据 |
| 可扩展性 | 文献库容量 | 支持至少5000篇文献和10000条标注 |
| 安全性 | AI API Key | 存储在.env中，不暴露到前端，仅后端调用 |
| 安全性 | 文件访问 | PDF文件通过API访问，不直接暴露文件路径；单用户无需登录鉴权 |
| 兼容性 | 浏览器支持 | Chrome 90+, Firefox 90+, Edge 90+, Safari 15+ |
| 部署 | 服务器部署 | Node后端 + SQLite + 文件系统，部署在服务器上通过浏览器访问。单用户使用，无需登录系统 |
| 国际化 | 界面语言 | 优先中文，架构支持扩展英文 |
| AI模型 | 国内大模型 | 使用DeepSeek/Qwen API，无需海外网络访问；架构预留多模型切换能力 |

---

## 8. 开发里程碑规划

### Phase 1 — 核心闭环（MVP）

**目标**: 从导入PDF到生成写作段落的完整链路可用

| 功能 | 说明 |
|---|---|
| 文献导入 | 单文件PDF导入 + 纯本地元数据自动提取（pdf-parse + 正则推断）+ 手动编辑 |
| 文献列表 | 集合树 + 文献表格视图 + 基础筛选 |
| PDF预览 | PDF.js渲染 + 文本选择 |
| 高亮标注 | 选择文字→高亮→附加批注→分配标签，标注颜色=标签颜色 |
| 标签管理 | 创建/编辑/删除标签（含颜色），按标签查看标注 |
| 标注颜色联动 | 标注视觉颜色跟随标签颜色，无标签时灰色 |
| 跨文献标注视图 | 选择标签查看所有关联标注 |
| 写作工作区 | 素材选择(标签)→写作风格(仿写段落/期刊风格/自定义提示词)→生成输出(段落+引用)，三区面板布局 |
| 双语输出 | 中英双语可选，引用格式随语言自动切换 |

### Phase 2 — 效率增强

| 功能 | 说明 |
|---|---|
| 批量文件夹导入 | 扫描文件夹批量导入PDF |
| 标注批量操作 | 多选标注批量添加/移除标签 |
| 写作风格自定义 | 用户创建自己的风格模板 |
| 生成历史 | 保存和回溯历史生成结果 |
| 标注导出 | 导出标注为文本/Markdown |
| DOI辅助元数据 | 提供DOI后调用Crossref API补充元数据（渐进增强） |
| 导入冲突处理 | 同名/同DOI文件重复导入时的选择处理 |

### Phase 3 — 高级功能

| 功能 | 说明 |
|---|---|
| 下划线/删除线标注 | 扩展标注类型 |
| 标签层级 | 标签嵌套树形组织 |
| 全文搜索 | 搜索PDF全文内容 |
| 多AI模型切换 | 支持Claude/GPT-4等其他模型 |
| 协作（远期） | 多用户共享文献库，需要登录和数据隔离 |
| 浏览器扩展 | 从网页一键导入文献 |

---

## 9. 风险与应对

| 风险 | 影响 | 应对策略 |
|---|---|---|
| PDF元数据提取准确率低 | 用户需大量手动修正，体验差 | 三级降级策略+置信度标注+便捷编辑界面；Phase 2可接入Crossref API通过DOI补充元数据 |
| PDF.js标注覆盖层开发 | 自实现高亮覆盖层与PDF.js文本层交互有技术难度 | 标注覆盖层使用SVG叠加方式，与PDF.js文本选择层分离渲染；优先保证高亮标注可用 |
| 标注颜色联动复杂度 | 标注属于多个标签时颜色切换逻辑需处理好 | 规则明确：显示第一个标签颜色，标签列表顺序可拖拽调整 |
| DeepSeek/Qwen API稳定性 | 国内模型API偶有波动或限流 | 架构预留多模型接口，可快速切换到备用模型；本地缓存生成结果减少重复调用 |
| DeepSeek/Qwen写作质量 | 国内模型学术写作质量可能略低于GPT-4 | Prompt中强约束写作规范；支持重新生成和手动编辑结果；后续可扩展接入GPT-4 |
| AI API成本 | 大量生成调用产生费用 | 每次生成聚合多个标注为一次调用（非逐条）；DeepSeek/Qwen价格已显著低于GPT-4 |
| 大PDF渲染性能 | 超长PDF可能卡顿 | PDF.js虚拟滚动+懒加载页面；设置页面渲染上限 |

---

## 10. 附录

### 10.1 引用格式模板定义

**APA格式**:
```
Author, A. A., & Author, B. B. (Year). Title of article. Title of Journal, Volume(Issue), Pages. https://doi.org/xxxxx
```

**IEEE格式**:
```
[1] A. A. Author and B. B. Author, "Title of article," Title of Journal, vol. XX, no. XX, pp. XXX-XXX, Month Year.
```

**GB/T 7714格式**:
```
作者. 标题[J]. 期刊名, 年, 卷(期): 起止页码. DOI: xxxxx
```

**Nature格式**:
```
Author, A. A. et al. Title of article. Nature XXX, XXX–XXX (Year).
```

### 10.2 预置写作风格Prompt模板

**中文文献综述**:
```
你是一位中文学术写作助手。根据提供的标注素材，撰写一段文献综述段落。
要求：
1. 段落结构：研究背景 → 国内外研究现状 → 主要发现与争议 → 研究趋势
2. 每个论点必须引用来源文献，使用[序号]标记
3. 语言风格：学术性、客观、逻辑严密，避免口语化表达
4. 严格基于提供的标注素材，不虚构未提及的内容
5. 引用格式：GB/T 7714标准
6. 段落长度：300-500字
7. 段末列举所有引用文献的完整信息
```

**Nature综述风格**:
```
You are an academic writing assistant. Based on the provided annotation materials, write a review paragraph in Nature style.
Requirements:
1. Structure: broad context → specific findings → critical assessment → outlook
2. Each claim must cite its source using [number] markers
3. Tone: authoritative yet accessible, concise, data-driven
4. Strictly based on provided annotations — no fabrication
5. Citation format: Nature style (Author et al. Nature XXX, XXX–XXX; Year)
6. Length: 200-400 words
7. List full references at the end
```

---

## 11. 需求确认记录

### 11.1 确认决策汇总（2026-06-02）

| 决策项 | 选项 | 最终结论 | 影响 |
|---|---|---|---|
| Zotero仿照程度 | 完全仿照 / 核心理念仿照布局可调 / 仅参考文献管理 | **核心理念仿照，布局自由设计** | UI设计灵活度更高，可优化标注和写作面板布局 |
| "选择文本"含义 | 库内选段落 / 外部粘贴 / 两种都支持 | **两种方式都支持** | 写作生成面板需同时支持：① 从库中文献选择参考段落 ② 外部粘贴参考文本 |
| 标注画线类型 | 高亮/下划线/删除线/自由画线 | **仅文本高亮（色块覆盖）** | MVP标注系统大幅简化，覆盖层实现仅需处理高亮 |
| 标注颜色逻辑 | 标注颜色=标签颜色 / 独立 / 默认跟随可覆盖 | **标注颜色 = 标签颜色** | 标注颜色不独立存储，由关联标签派生。修改标签颜色→所有标注同步更新 |
| 生成内容范围 | 单段论述 / 多段综述 / 两者都支持 | **单段论述段落** | AI Prompt可更聚焦，生成逻辑简化 |
| 输出语言 | 仅中文 / 仅英文 / 中英双语可选 | **中英双语可选** | 写作风格模板需同时包含中英文版本；引用格式随语言切换 |
| 部署方式 | 本地单机 / 服务器 / 先本地后扩展 | **服务器部署** | 需考虑服务器运行环境；SQLite仍可用（单用户）；前后端分离部署 |
| 用户数量 | 单用户 / 小团队 / 多用户独立库 | **单用户（仅自己）** | 无需登录鉴权系统；无数据隔离需求；大幅简化后端 |
| AI模型 | OpenAI / Claude / 多模型可选 / 国内模型 | **DeepSeek/Qwen（国内模型）** | 使用国内API无网络障碍；价格更低；Prompt适配国内模型特性 |
| PDF元数据 | 纯本地 / Grobid / 本地+Crossref | **纯本地提取** | 仅依赖pdf-parse+正则，无外部服务依赖；准确率约70-80%，需手动补充 |

### 11.2 关键设计约束（源自确认结果）

1. **标注颜色不可手动设置**: 所有标注颜色由标签颜色派生。这是核心设计决策，影响数据库schema（annotations表不存储颜色，前端动态计算）和UI交互（标注创建流程中不出现颜色选择器）。
2. **无用户鉴权**: 单用户服务器部署，无需登录、注册、权限管理。但API层仍需做基本防护（如IP白名单或简单token），防止意外暴露。
3. **国内模型优先**: DeepSeek和Qwen的API调用方式与OpenAI类似（兼容OpenAI SDK格式），但Prompt设计需适配中文能力更强的特点。
4. **写作风格三模式**: 写作风格选择面板需同时支持三种输入：①预置风格模板下拉选择 ②从库中文献选择参考段落 ③外部粘贴参考文本。后两种需调用AI先分析文本风格，再作为生成指令的一部分。

---

---

## 11. 需求确认记录

> **版本**: v1.0  
> **日期**: 2026-06-02  
> **状态**: 需求已确认

### 确认决策清单

| # | 决策项 | 认结论 | 影响范围 |
|---|---|---|---|
| 1 | Zotero仿照程度 | 核心理念仿照，布局自由设计 | 不强制复刻Zotero UI，可优化布局 |
| 2 | 写作风格参考来源 | 仿写段落 + 期刊风格 + 自定义提示词，三模式 | 写作工作区风格区需支持三模式切换和组合 |
| 5 | 生成内容范围 | 单段论述段落 | 每次生成一个200-500字段落，含引用 |
| 3 | 标注类型 | 仅文本高亮（色块覆盖） | 数据模型简化，交互流程简化 |
| 4 | 标注颜色逻辑 | 标注颜色 = 标签颜色（自动跟随） | annotations表无需独立color管理，前端动态计算 |
| 4b | 写作工作区架构 | 素材区→风格区→输出区三区面板 | 不是一次性弹窗而是持久面板，三模式风格选择 |
| 5 | 生成内容范围 | 单段论述段落 | 每次生成一个200-500字段落 |
| 6 | 输出语言 | 中英双语可选 | 引用格式和Prompt模板需适配两种语言 |
| 7 | 部署方式 | 服务器部署，单用户 | 无需登录鉴权系统，SQLite足够 |
| 8 | AI模型 | DeepSeek/Qwen（国内模型） | Prompt适配国内模型API，架构预留多模型接口 |
| 9 | PDF元数据提取 | 纯本地提取（pdf-parse + 正则） | 准确率70-80%，所有字段可手动编辑 |
| 10 | 标注颜色联动 | 无标签→灰色(#9E9E9E)；有标签→第一个标签颜色；多标签→第一个标签颜色 | 需前端实现标签颜色→标注颜色的实时联动渲染 |

*文档结束 — LitWrite 产品需求规格 v1.0*